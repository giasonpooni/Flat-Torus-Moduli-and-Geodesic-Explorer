# Validation pins

These files are the end of a tool run. Chat text is not a pin.

Status: in-development. Not released. Re-running `run_first_release` and
`run_fold_experiment` must reproduce the commitment digests in this directory.

A function is not out of development because a demo printed PASS.
